#include "scanner.h"
#include <iostream>

void scanner(char* buffer) {
    // Will implement scanner later

    // Print a message indicating that the function was called
    std::cout << "Buffer passed to Scanner!" << std::endl;
}
